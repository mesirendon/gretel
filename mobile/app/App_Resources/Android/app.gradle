// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    multiDexEnabled true
    applicationId = "app.web.greteltracer.mobile"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
  project.ext {
    googlePlayServicesVersion = "15.0.0"
  }
}
